pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
         }
       stage('Environment')
         steps{
          echo '$workspace'
          echo '$JENKINS_URL'
         }
      }
   }
}
